name: matrimony_app
description: "MATRIMONY - ADARSH PRADEEP"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cloud_firestore: ^4.15.0
  cupertino_icons: ^1.0.8
  firebase_auth: ^4.17.4
  firebase_core: ^2.27.0
  flutter:
    sdk: flutter
  fluttertoast: ^8.2.4
  google_fonts: ^6.1.0
  http: ^1.0.0
  image_picker: ^1.0.2

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/background_image.jpg
    - assets/images/male.avif
